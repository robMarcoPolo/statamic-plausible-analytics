<div class="card p-0 overflow-hidden h-full">
    <plausible-visitor-overview
        period="{{ $period }}"
        :widget="true"
        :show-graph="{{ $show_graph ? 'true' : 'false' }}"
    />
</div>
